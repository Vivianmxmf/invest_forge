# InvestForge runtime dependencies (server-side install).
# Local dev needs only: pandas, numpy, pytest, pydantic, langgraph (+ langchain core),
# everything heavier (transformers/peft/qdrant/qlib/streamlit) is gated behind
# lazy imports.

# --- Core data + numerics ---
pandas>=2.1,<3.0
numpy>=1.26,<3.0
scipy>=1.11

# --- LLM / Agent framework ---
langgraph>=0.2,<0.4
langchain>=0.2,<0.4
langchain-core>=0.2,<0.4
langchain-community>=0.2,<0.4
langchain-openai>=0.1
langchain-anthropic>=0.1

# --- RAG stack ---
qdrant-client>=1.7,<2.0
langchain-qdrant>=0.1
rank-bm25>=0.2
sentence-transformers>=2.7
ragas>=0.1.7,<0.3
pymupdf>=1.24

# --- Market data ---
tushare>=1.4,<2.0
akshare>=1.13,<2.0

# --- Backtest ---
alphalens-reloaded>=0.4
# pyqlib install is brittle on macOS; pin only on server (server_migrate.sh handles it)
# pyqlib>=0.9
xgboost>=2.0,<3.0
scikit-learn>=1.4

# --- Finetune / inference (server-side, A5000 24GB × 4) ---
# Comment out on laptops; server_migrate.sh installs these gated on nvidia-smi.
# transformers>=4.41
# accelerate>=0.30
# peft>=0.11
# trl>=0.9
# bitsandbytes>=0.43
# vllm>=0.5

# --- API / frontend ---
fastapi>=0.111
uvicorn[standard]>=0.30
streamlit>=1.30,<2.0
plotly>=5.18,<7.0
matplotlib>=3.8,<4.0
reportlab>=4.0,<5.0  # PDF research-report export (uses Adobe CID font, CJK-ready, no TTF needed)
qrcode[pil]>=7.4,<9.0  # Shareable analysis QR codes (DECISION tab "SHARE THIS" expander)
streamlit-autorefresh>=1.0  # 30 s tick-tape refresh in status bar

# --- Multimodal image-input validation (CPU-only, light) ---
Pillow>=10.0,<12.0

# --- Observability ---
langsmith>=0.1
prometheus-client>=0.19
python-json-logger>=2.0

# --- Misc ---
python-dotenv>=1.0
typer>=0.9
rich>=13.7
pydantic>=2.7
pydantic-settings>=2.2

# --- Dev / test ---
pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.12
pytest-asyncio>=0.23
hypothesis>=6.92
black>=24.1
ruff>=0.2
mypy>=1.8
